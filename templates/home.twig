<!doctype html>
<html lang="en">
    <head>
        <title></tile>
        <meta name ="viewport" content="width=device-width,initial-scale=1">
        <link rel="stylesheet" href="node_modules/bootstrap/dist/css/bootstrap.min.css">
         <script scr="node_modules/jquery/dist/jquery.min.js"></script>
         <script src="node_modules/poper.js/dist/umd/poper.min.js"></script>
        <script scr="node_modules/bootstrap/dist/js/bootstrap.min.js"></script>
    </head>
    
    <body>
        <main class="container">
            <div class="row">
                {% for product in product%}
                <div class="col-md-3" data-id="{{product.product_id}}">
                    <h3>{{product.name}}</h3>
                    <h6>{{product.price}}</h6>
                    <p>{{product.description}}</p>

                {% endfor %}
            </div>
        </main>
    </body>
</html>